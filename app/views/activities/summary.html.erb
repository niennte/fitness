<div id="notice" class="text-center <%= notice.present? ? 'alert alert-success' : ''%>" role="alert"><%= notice %></div>
<section class="py-5 container position-relative">
  <%= link_to 'All Time', summary_all_path, :class => "position-absolute auxiliary-link-right badge badge-light" %>
  <%= link_to 'Weekly', summary_path, :class => "position-absolute auxiliary-link-left badge badge-primary" %>
  <h1 class="text-center">Summary</h1>

  <%= render 'weekly_header', weeks_ago: @weeks_ago %>

  <%= render 'summary', summary: @summary %>

  <nav class="nav justify-content-center">
    <%= link_to 'Activities', activities_path({ weeks_ago: @weeks_ago }), :class => "nav-item nav-link" %>
  </nav>

</section>